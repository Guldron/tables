;
(function(){
        'use strict';

        function getStatitic(data) {

                return {
                        getTotalLinks: getTotalLinks,
                        getCarts: getCarts,
                        getReports: getReports,
                        getVerticals: getVerticals
                        }
                }

        function getTotalLinks() {
                var links;
                return links;
        }

        function getCarts() {
                var links;
                return links;
        }

        function getReports() {
                var links;
                return links;
        }

        function getVerticals() {
                var links;
                return links;
        }


        angular
                .module('app')
                .factory('InboxService', InboxService);

})();